package Interfaces;
/**
 * Interface representing a password.
 */
public enum PasswordType {
    /**
     * Enum representing the type of the password.
     */
    STANDART,
    /**
     * Enum representing the type of the password.
     */
    STRONG
}
